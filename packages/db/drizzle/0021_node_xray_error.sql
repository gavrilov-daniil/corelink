-- Почему Xray на ноде не обслуживает клиентов, со слов агента: не поднялся после
-- рестарта (чужой процесс держит порт) или упал позже. NULL — работает. Без этого
-- поля нода с лежащим Xray считалась сошедшейся по одному хешу конфига, и админка
-- показывала её рабочей.
ALTER TABLE "node_reported_state" ADD COLUMN IF NOT EXISTS "xray_error" text;
